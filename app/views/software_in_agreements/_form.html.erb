<%= render 'shared/settings_container'%>
<%= form_for(@software_in_agreement) do |f| %>
  <% if @software_in_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@software_in_agreement.errors.count, "error") %> prohibited this software_in_agreement from being saved:</h2>

      <ul>
      <% @software_in_agreement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :software_id %><br>
    <%= f.select :software_id, options_from_collection_for_select(@software, :id, :name) %>
  </div>
  <div class="field">
    <%= f.hidden_field :agreement_id %>
  </div>
  <div class="field">
    <%= f.label :monthly_price %><br>
    <%= f.number_field :monthly_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

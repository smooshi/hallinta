<p id="notice"><%= notice %></p>
<%= render 'shared/companies_container' %>
<h1>Listing Company Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @company_types.each do |company_type| %>
      <tr>
        <td><%= link_to company_type.name, company_type %></td>
        <td><%= company_type.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Company type', new_company_type_path %>

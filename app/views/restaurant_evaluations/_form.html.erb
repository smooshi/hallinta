<%= form_for(@restaurant_evaluation) do |f| %>
  <% if @restaurant_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant_evaluation.errors.count, "error") %> prohibited this restaurant_evaluation from being saved:</h2>

      <ul>
      <% @restaurant_evaluation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.select :restaurant_id, options_from_collection_for_select(@restaurants, :id, :name) %>
    </div>
    <div class="field">
      <%= f.select :restaurant_evaluation_type_id, options_from_collection_for_select(@restaurant_evaluation_types, :id, :name) %>
    </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="field">
    <%= f.label :evaluation_comment %><br>
    <%= f.text_area :evaluation_comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

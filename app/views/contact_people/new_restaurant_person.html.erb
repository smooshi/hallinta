<h1>New Restaurant Contact Person</h1>

<%= render 'form' %>

<div class="field">
  <%= f.label :restaurant_id %><br>
  <%= f.select :restaurant_id, options_from_collection_for_select(@restaurants, :id, :name) %>
</div>
<%= form_for(@other_billing_in_agreement) do |f| %>
  <% if @other_billing_in_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@other_billing_in_agreement.errors.count, "error") %> prohibited this other_billing_in_agreement from being saved:</h2>

      <ul>
      <% @other_billing_in_agreement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :agreement_id %><br>
    <%= f.number_field :agreement_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :total_price %><br>
    <%= f.number_field :total_price %>
  </div>
  <div class="field">
    <%= f.label :monthly_price %><br>
    <%= f.number_field :monthly_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
